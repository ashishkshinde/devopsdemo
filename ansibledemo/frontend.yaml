name: Frontend App
hosts: localhost 
become: yes

tasks:
  - name: Install the latest version of NGINX
    ansible.builtin.dnf:
      name: nginx
      state: latest

  - name: enable default nginx
    ansible.builtin.systemd_service:
      name: nginx
      state: started
      enabled: true